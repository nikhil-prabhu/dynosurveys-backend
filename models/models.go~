package models

import (
        "github.com/jinzhu/gorm"
)

// User data
type User struct {
        gorm.Model

        Name    string
        Email   string `gorm:"type:varchar(100);unique_index"`
}
